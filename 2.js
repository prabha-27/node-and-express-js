const Name=' chandra';
let age=25;
const nativeiscbe=true;
age=26;
const summarizeUser =(userName,userAge,nativeIsCoimbatore)=>{

this

    return (
        'Name is' +
         userName+
         ',age:'+ 
         userAge+
         ' and native is cbe ' +
         nativeIsCoimbatore
        );
};
const addOne =a=>a+1;

console.log(addOne(1));
const addRandom =()=>1+2;
console.log(addRandom());
console.log(summarizeUser(Name,age,nativeiscbe));
